{
  "name": "taskade-mcp-monorepo",
  "private": true,
  "license": "MIT",
  "packageManager": "yarn@1.22.19",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "yarn workspace @taskade/mcp-server build",
    "start": "yarn workspace @taskade/mcp-server start",
    "dev": "cd packages/server && yarn run dev",
    "generate:taskade-mcp-tools": "yarn workspace @taskade/mcp-server generate:taskade-mcp-tools"
  }
}
